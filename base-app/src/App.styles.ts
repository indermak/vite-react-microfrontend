import styled from 'styled-components';

const LinkSection = styled.div({
	backgroundColor: 'black',
});

const RandomSection = styled.div({
	color: 'grey',
});

export { LinkSection, RandomSection };
